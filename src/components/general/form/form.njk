{% from "components/general/button/button.njk" import render as button %}
{% from "components/general/select/select.njk" import render as select %}
{% from "components/general/input/input.njk" import render as input %}
{% from "components/general/textarea/textarea.njk" import render as textarea %}
{% from "components/general/consent-checkbox/consent-checkbox.njk" import render as consentCheckbox %}
{% from "components/general/input-file/input-file.njk" import render as inputFile %}

<form class="form" data-validate>
    <div class="grid col-2">
        <div class="field">
            <label class="label" for="feedback-request">Выберите обращение</label>
            {{ select(
                id="feedback-request",
                name="request",
                placeholder="Выберите обращение",
                options=[
                    { value: "consultation", text: "Название обращения" },
                    { value: "order", text: "Название обращения 2" },
                    { value: "other", text: "Название обращения 3" }
                ]
            ) }}
        </div>
        <div class="field">
            <label class="label" for="feedback-name">Ваше имя</label>
            {{ input(
                type="text",
                id="feedback-name",
                name="name",
                placeholder="Ваше имя*",
                autocomplete="name",
                required=true,
                minlength="2",
                data={"error": "Введите имя (минимум 2 буквы)"}
            ) }}
        </div>
        <div class="field">
            <label class="label" for="feedback-message">Сообщение</label>
            {{ textarea(
                id="feedback-message",
                name="message",
                rows="6",
                placeholder="Сообщение"
            ) }}
        </div>
        <div class="field">
            <label class="label" for="feedback-phone">Ваш телефон</label>
            {{ input(
                type="tel",
                id="feedback-phone",
                name="phone",
                placeholder="Ваш телефон*",
                autocomplete="tel",
                data={"mask": "phone", "error": "Введите полный номер телефона"},
                required=true
            ) }}
        </div>
        <div class="field">
            <label class="label" for="feedback-email">E-mail</label>
            {{ input(
                type="email",
                id="feedback-email",
                name="email",
                placeholder="E-mail*",
                autocomplete="email",
                data={"error": "Неверный формат"},
                required=true
            ) }}
        </div>
        <div class="field-file">
            {{ inputFile(
                id="feedback-file",
                accept=".pdf,.jpg,.jpeg,.png",
                maxSize=1024,
                errorSize="Максимальный размер файла 1MB",
                errorType="Допустимы только PDF, JPG, PNG",
                placeholder="Прикрепить файл",
                hint="(не более 1MB, pdf, jpg)"
            ) }}
        </div>
        <div class="field-consent">
            {{ consentCheckbox(
                id="feedback-consent",
                class="variant",
                data={"error": "Необходимо отметить"},
                required=true
            ) }}
        </div>
        <div class="actions">
            {{ button(
                text="Отправить",
                type="submit",
                disabled=true
            ) }}
        </div>
    </div>
</form>